apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pricing-api-pr-1
  namespace: argocd
  labels:
    team: red
    environment: preview
    pr-number: "1"
    ephemeral: "true"
    app.kubernetes.io/part-of: red
  annotations:
    description: "Preview environment for PR #1"
    preview-url: "https://pricing-api-pr-1.red.chrishouse.io"
    pr-url: "https://github.com/crh225/pricing-api/pull/1"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: applications

  source:
    repoURL: https://github.com/crh225/pricing-api
    targetRevision: pr
    path: helm
    helm:
      releaseName: pricing-api-pr-1
      valueFiles:
        - values.yaml
      parameters:
        - name: image.repository
          value: ghcr.io/crh225/pricing-api
        - name: image.tag
          value: "pr-1"
        - name: namespace
          value: red-dev-pr-1
        - name: serviceMonitor.enabled
          value: "false"
        - name: istio.enabled
          value: "true"
        - name: istio.preview.enabled
          value: "true"
        - name: istio.preview.hostname
          value: pricing-api-pr-1.red.chrishouse.io

  destination:
    name: shared-dev-cluster
    namespace: red-dev-pr-1

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false  # Namespace created by NamespaceClaim
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
