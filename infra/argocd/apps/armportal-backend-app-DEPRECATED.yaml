# [DEPRECATED] Argo CD Application for ARM Portal Backend API
#
# DEPRECATED: The backend has been migrated to Azure Container Apps.
# This ArgoCD app is kept for reference only and should NOT be applied to the cluster.
# See: infra/environments/dev/backend-containerapps.tf for the current backend deployment.
#
# To fully remove from cluster (if still deployed):
#   kubectl delete application armportal-backend -n argocd
#   kubectl delete namespace armportal-backend
#
# Original: Tracked backend deployment manifests from the GitHub repository

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: armportal-backend-DEPRECATED
  namespace: argocd
  annotations:
    # Mark as deprecated - DO NOT APPLY
    argocd.argoproj.io/sync-wave: "-999"
    deprecated: "true"
    deprecation-notice: "Migrated to Azure Container Apps - see backend-containerapps.tf"
spec:
  project: default

  source:
    repoURL: https://github.com/crh225/ARMServicePortal.git
    targetRevision: main
    path: infra/kubernetes/armportal-backend-DEPRECATED

  destination:
    server: https://kubernetes.default.svc
    namespace: armportal-backend-deprecated

  # DISABLED - No automated sync
  # syncPolicy:
  #   automated:
  #     prune: true
  #     selfHeal: true
  #     allowEmpty: false
  #   syncOptions:
  #     - CreateNamespace=true
  #   retry:
  #     limit: 5
  #     backoff:
  #       duration: 5s
  #       factor: 2
  #       maxDuration: 3m
