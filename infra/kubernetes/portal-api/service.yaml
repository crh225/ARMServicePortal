apiVersion: v1
kind: Service
metadata:
  name: portal-api
  namespace: portal-api
  labels:
    app.kubernetes.io/name: portal-api
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 4000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: portal-api
    app.kubernetes.io/component: backend
