# Production Deployment: ${{ values.serviceName }}

These manifests were generated by the Backstage "Promote to Production" template.

## What's Included

- **namespace-claim.yaml**: Crossplane NamespaceClaim that provisions the `${{ values.namespace }}` namespace on aks-app-spoke
- **argocd-application.yaml**: ArgoCD Application that deploys from your existing repository

## Ingress (in separate folders)

- **istio-hub/**: VirtualService on hub cluster to route `${{ values.serviceName }}-prod.chrishouse.io` to spoke
- **istio-spoke/**: VirtualService on spoke cluster to route to service

## Source Repository

${{ values.repoUrl }}

## Deployment Target

- **Cluster**: aks-app-spoke (production)
- **Namespace**: ${{ values.namespace }}
- **URL**: https://${{ values.serviceName }}-prod.chrishouse.io
- **Replicas**: ${{ values.replicas }}
- **HPA Enabled**: ${{ values.enableHPA }}
