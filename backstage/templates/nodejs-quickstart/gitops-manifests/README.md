# GitOps Manifests for ${{ values.serviceName }}

These manifests are automatically submitted as a pull request to the ARMServicePortal repository.

## Files

### namespace-claim.yaml
Creates an isolated namespace for the `${{ values.team }}` team on the shared development cluster.

**What it creates:**
- Namespace: `${{ values.namespace }}`
- ResourceQuota: Team-wide limits (2 CPU, 4GB RAM)
- LimitRange: Default resource limits for pods
- NetworkPolicy: Default-deny ingress (security isolation)

### argocd-application.yaml
Configures ArgoCD to continuously deploy the service from the GitHub repository.

**What it does:**
- Watches `${{ values.repoUrl }}` for changes
- Deploys using Helm chart from `/helm` directory
- Auto-syncs on every git push to main
- Auto-heals if manual changes are made to cluster

## Deployment Flow

1. **PR Submitted**: These files are added to `infra/quickstart-services/${{ values.serviceName }}/`
2. **PR Merged**: ArgoCD detects new manifests
3. **Namespace Created**: Crossplane provisions isolated namespace
4. **App Deployed**: ArgoCD deploys the service to the new namespace
5. **Ready**: Service is live at `http://${{ values.serviceName }}.shared-dev.internal`

**Estimated Time:** 2-3 minutes after PR merge

---

🥚 Generated by Golden Path Template
